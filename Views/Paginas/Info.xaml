<Page x:Class="TVPGestion_IPO.Views.Ayuda"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Ayuda" Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Viewbox Grid.Row="0" 
                 Stretch="Uniform" 
                 HorizontalAlignment="Left"
                 MaxHeight="60"
                 Margin="40,10,28,10">
            <Label Content="Acerca de" 
                   FontSize="25" 
                   FontWeight="Bold"/>
        </Viewbox>

        <Border Grid.Row="1" Margin="40,10,40,27" 
                BorderBrush="#ccc" BorderThickness="1" CornerRadius="5" Background="#F9F9F9" Grid.RowSpan="2">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:PackIcon Kind="InformationOutline" Width="60" Height="60" 
                                         Foreground="#065386" VerticalAlignment="Center" Margin="0,0,20,0"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Gestión de Productos e Inventario" FontWeight="Bold" FontSize="20" Foreground="#065386"/>
                    <TextBlock Text="Versión 1.0.0 " FontSize="16" Margin="0,5,0,0"/>
                    <Separator Margin="0,10"/>
                    <TextBlock Text="Desarrollado por:" FontWeight="SemiBold" FontSize="16"/>
                    <TextBlock Margin="0,2" FontSize="16"><Run Text="["/><Run Language="es-es" Text="Belén Huertas Ruiz"/><Run Text=" / "/><Run Language="es-es" Text="Lucía Sánchez-Chiquito Gómez/ Álvaro Márquez "/><Run Language="es-es" Text=" Gavilán"/><Run Text="]"/></TextBlock>
                    <TextBlock Text="Asignatura: Interacción Persona-Ordenador" FontStyle="Italic" Foreground="Gray" FontSize="16"/>
                    <TextBlock Margin="0,2" FontSize="14"><Run Text="Fecha: Enero 202"/><Run Language="es-es" Text="6"/></TextBlock>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>