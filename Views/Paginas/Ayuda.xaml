<Page x:Class="TVPGestion_IPO.Views.Ayuda"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Ayuda" Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Viewbox Grid.Row="0" 
                 Stretch="Uniform" 
                 HorizontalAlignment="Left"
                 MaxHeight="60"
                 Margin="40,10,28,10">
            <Label Content="Ayuda y Acerca de" 
                   FontSize="25" 
                   FontWeight="Bold"/>
        </Viewbox>

        <Border Grid.Row="1" Margin="40,10,40,20" 
                BorderBrush="#ccc" BorderThickness="1" CornerRadius="5" Background="#F9F9F9">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:PackIcon Kind="InformationOutline" Width="60" Height="60" 
                                         Foreground="#065386" VerticalAlignment="Center" Margin="0,0,20,0"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Gestión de Productos e Inventario" FontWeight="Bold" FontSize="18" Foreground="#065386"/>
                    <TextBlock Text="Versión 1.0.0 " FontSize="14" Margin="0,5,0,0"/>
                    <Separator Margin="0,10"/>
                    <TextBlock Text="Desarrollado por:" FontWeight="SemiBold"/>
                    <TextBlock Margin="0,2"><Run Text="["/><Run Language="es-es" Text="Belén Huertas Ruiz"/><Run Text=" / "/><Run Language="es-es" Text="Lucía Sánchez-Chiquito Gómez/ Álvaro Márquez "/><Run Language="es-es" Text=" Gavilán"/><Run Text="]"/></TextBlock>
                    <TextBlock Text="Asignatura: Interacción Persona-Ordenador" FontStyle="Italic" Foreground="Gray"/>
                    <TextBlock Margin="0,2"><Run Text="Fecha: Enero 202"/><Run Language="es-es" Text="6"/></TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <GroupBox Grid.Row="2" Header="Guía Rápida de Iconos" Margin="40,0,40,40"
                  BorderBrush="#065386"
                  Style="{StaticResource MaterialDesignCardGroupBox}">
            <GroupBox.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontWeight="Bold" Foreground="White" Margin="10"/>
                </DataTemplate>
            </GroupBox.HeaderTemplate>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10">
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="Pencil" Foreground="#065386" Width="24" Height="24" HorizontalAlignment="Center"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Editar Registro" FontWeight="Bold"/>
                            <TextBlock Text="Permite modificar la información de un cliente, producto o pedido existente." TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="Delete" Foreground="#065386" Width="24" Height="24" HorizontalAlignment="Center"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Eliminar Registro" FontWeight="Bold"/>
                            <TextBlock Text="Borra permanentemente el elemento seleccionado. Se pedirá confirmación antes de borrar." TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="Add" Foreground="Gray" Width="24" Height="24" HorizontalAlignment="Center"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Añadir Nuevo" FontWeight="Bold"/>
                            <TextBlock Text="Abre el formulario para registrar una nueva entrada en la base de datos." TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</Page>